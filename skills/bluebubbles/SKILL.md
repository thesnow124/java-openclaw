---
name: bluebubbles
description: Use when you need to send or manage iMessages via BlueBubbles (recommended iMessage integration). Calls go through the generic message tool with channel="bluebubbles".（中文入口）
user-invocable: true
disable-model-invocation: false
---
# 技能说明（中文入口）

该技能已从 OpenClaw 上游同步。

## 使用规则
1. 先读取同目录 `SKILL.upstream.en.md`。
2. 严格遵循其中的全部步骤、约束、安全要求与输出格式。
3. 若本文件与上游文件有冲突，以上游文件为准。
4. 与用户沟通时默认使用中文；命令、路径、代码、JSON 键名保持原样。
5. 在执行前先确认依赖与环境条件，再按上游流程完成任务。

## 说明
- 该中文入口用于触发与路由。
- 权威技能内容维护在 `SKILL.upstream.en.md`。
